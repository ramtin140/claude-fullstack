# FIFA Soul — پلتفرم تورنمنت‌های فوتبال دیجیتال

یک اپلیکیشن فول‌استک برای برگزاری تورنمنت‌ها و مسابقات آنلاین فیفا، با طراحی الهام‌گرفته از قالب گرافیکی FIFA Soul (تم بنفش/طلایی، راست‌به‌چپ، فونت Vazirmatn).

## معماری

```
.
├── backend/   # Node.js + Express + SQLite (REST API, JWT auth)
└── frontend/  # React + Vite (RTL, Persian UI)
```

- **بک‌اند**: Express روی SQLite (`better-sqlite3`) با احراز هویت JWT، نقش‌های `member`/`admin`، و مسیرهای REST برای تورنمنت‌ها، مسابقات، اخبار، آمار، کاربران و خبرنامه.
- **فرانت‌اند**: React 18 + React Router با فونت متغیر Vazirmatn (سبک‌ترین فونت فارسی با پوشش کامل وزن‌ها در یک فایل) و آیکون‌های سبک `lucide-react`.
- **پنل مدیریت**: مسیر `/admin` (فقط برای نقش admin) برای مدیریت کامل تورنمنت‌ها، مسابقات، اخبار و کاربران.

## اجرا در محیط توسعه

### ۱. بک‌اند

```bash
cd backend
npm install
cp .env.example .env   # در صورت نیاز مقادیر را تغییر دهید
npm run dev             # http://localhost:4000
```

اولین اجرا به‌صورت خودکار دیتابیس SQLite را در `backend/data/fifasoul.db` می‌سازد و با داده‌های نمونه seed می‌کند.

کاربر ادمین پیش‌فرض: `admin@fifasoul.test` / `admin123`

### ۲. فرانت‌اند

```bash
cd frontend
npm install
npm run dev              # http://localhost:5173
```

درخواست‌های `/api/*` در حالت توسعه به‌صورت خودکار به بک‌اند (پورت ۴۰۰۰) پروکسی می‌شوند.

## دیپلوی روی Railway (دو سرویس جدا)

بک‌اند و فرانت‌اند به‌صورت دو سرویس مستقل روی Railway دیپلوی می‌شوند و از طریق HTTP با هم صحبت می‌کنند (نه سرو کردن فایل‌های استاتیک از یک سرویس).

### ۱. سرویس بک‌اند

- **Root Directory**: `backend`
- **Build Command**: `npm install`
- **Start Command**: `npm start`
- **Volume**: یک Volume پایدار روی مسیر `/data` متصل کنید (برای فایل SQLite)

متغیرهای محیطی سرویس بک‌اند:

| متغیر | مقدار |
|---|---|
| `JWT_SECRET` | یک رشته تصادفی و امن |
| `FRONTEND_URL` | آدرس کامل سرویس فرانت‌اند روی Railway، مثلاً `https://fifasoul-frontend.up.railway.app` (برای مجاز کردن CORS) |
| `PORT` | نیازی به تنظیم دستی نیست؛ Railway خودش مقداردهی می‌کند |
| `DATABASE_PATH` | اختیاری. اگر ست نشود و روی Railway اجرا شوید (با تشخیص خودکار متغیرهای `RAILWAY_*`)، مسیر پیش‌فرض `<مسیر mount شده ولوم>/fifasoul.db` است (یا `/data/fifasoul.db` در نبود آن متغیر). برای اطمینان کامل می‌توانید صریحاً `DATABASE_PATH=/data/fifasoul.db` را هم ست کنید |

### ۲. سرویس فرانت‌اند

- **Root Directory**: `frontend`
- **Build Command**: `npm install && npm run build`
- **Start Command**: یک static server برای پوشه `dist`، مثلاً `npx serve -s dist -l $PORT` (یا هر Railway static-site preset مشابه)

متغیر محیطی سرویس فرانت‌اند:

| متغیر | مقدار |
|---|---|
| `VITE_API_URL` | آدرس کامل سرویس بک‌اند روی Railway، مثلاً `https://fifasoul-backend.up.railway.app` (چون این مقدار در زمان build داخل باندل جاوااسکریپت جاسازی می‌شود، هر بار که آدرس بک‌اند عوض شد باید فرانت‌اند را دوباره build/دیپلوی کنید) |

اگر `VITE_API_URL` ست نشود، فرانت‌اند از مسیر نسبی `/api` استفاده می‌کند که فقط در حالت توسعه محلی (با پروکسی Vite) معنی دارد؛ برای دیپلوی این متغیر الزامی است.

### بعد از دیپلوی

با اولین اجرای بک‌اند، اسکیمای دیتابیس و داده‌های نمونه به‌صورت خودکار روی Volume ساخته می‌شوند. چون این دو سرویس روی دامنه‌های جدا هستند، بک‌اند باید `FRONTEND_URL` را برای CORS بشناسد و فرانت‌اند باید `VITE_API_URL` را برای پیدا کردن API بشناسد — هر دو باید دقیقاً با آدرس نهایی Railway هر سرویس هم‌خوانی داشته باشند.

## امکانات

- ثبت‌نام / ورود کاربران با رمزنگاری bcrypt و توکن JWT
- مدیریت تورنمنت‌ها (لیگ/کاپ) و ثبت‌نام کاربران در آن‌ها
- مدیریت مسابقات با وضعیت زنده/در انتظار/پایان‌یافته
- بخش اخبار با دسته‌بندی (بازی‌های فعال، جدیدترین‌ها، گیک‌ها، آموزش‌ها)
- آمار خانه (رو در رو، پلی‌آف، لیگ، کاپ) و جدول اعضای برتر — همگی از دیتابیس واقعی محاسبه می‌شوند
- عضویت در خبرنامه با شماره موبایل
- پنل مدیریت کامل با CRUD برای تورنمنت‌ها، مسابقات، اخبار و کاربران
- **هسته بازی رو-در-رو** (تیکت/اکسپرینس + گردش‌کار ثبت نتیجه) — شرح کامل در بخش زیر

## هسته بازی رو-در-رو (Head-to-Head)

پیاده‌سازی دقیق سناریوی توصیف‌شده در مستندات: دو بازیکن یک مسابقه دو نیم‌فصلی (رفت/برگشت) بازی می‌کنند، نتیجه را دستی ثبت می‌کنند، و در صورت اختلاف، کارشناس سایت نتیجه نهایی را ثبت می‌کند.

### مدل اقتصادی

- **تیکت**: واحد پول داخل سایت (هر تیکت = ۵ هزار تومان اعتبار). حداکثر ۵ تیکت به ازای هر مسابقه.
- **اکسپرینس (XP)**: به همه بازیکنان تعلق می‌گیرد؛ حالت "بازی رایگان" با XP به‌جای تیکت انجام می‌شود.
- **گریدبندی فصلی**: D (زیر ۳۰۰) / C (۳۰۰-۴۵۰) / B (۴۵۰-۶۰۰) / A (بالای ۶۰۰) — آستانه‌ها توسط ادمین قابل ویرایش است (`/api/admin/grade-thresholds`).
- برد = ۳۵ امتیاز فصلی، مساوی = ۲۰، باخت = ۰ (همزمان به‌عنوان XP هم اعمال می‌شود).
- بازنشانی فصلی (هر سه ماه): `POST /api/admin/season/reset` — وضعیت فعلی هر کاربر را در `season_archive` بایگانی می‌کند و امتیاز فصلی/برد/باخت را صفر می‌کند (اکسپرینس و موجودی تیکت تغییر نمی‌کند، چون آن‌ها انباشتی/دائمی هستند).

### گردش‌کار یک مسابقه

1. `POST /api/h2h` — ایجاد مسابقه باز (نوع شرط: تیکت یا XP، کنسول، ورژن بازی، گزینه خصوصی+رمز عبور)
2. `POST /api/h2h/:id/join` — حریف جوین می‌شود؛ بررسی رمز عبور (در صورت خصوصی بودن) → بررسی موجودی کافی هر دو طرف → کسر تیکت از هر دو → **قفل شدن مسابقه** (دقیقاً مثل رزرو بلیط، دیگر کسی نمی‌تواند جوین شود) → دو نیم‌فصل (رفت/برگشت) ساخته می‌شود
3. `POST /api/h2h/:id/legs/:n/submit` — یکی از دو بازیکن نتیجه نیم‌فصل را ثبت می‌کند
4. حریف یا `POST .../confirm` می‌زند (نتیجه رسمی می‌شود) یا `POST .../dispute` می‌زند (نتیجه پیشنهادی خودش + مستندات را ثبت می‌کند)
5. در صورت اختلاف، ادمین/کارشناس از صف `GET /api/h2h/admin/expert-queue` نتیجه را می‌بیند و `POST .../expert-resolve` نتیجه نهایی و رسمی را ثبت می‌کند (با پرچم مخصوص «بررسی‌شده توسط کارشناس»)
6. وقتی هر دو نیم‌فصل نهایی شدند، مجموع گل‌ها محاسبه و برنده مشخص می‌شود؛ به هر دو بازیکن XP/امتیاز فصلی داده می‌شود و در مسابقات تیکتی، برنده کل استخر تیکت (۲×مبلغ شرط) را می‌برد (در تساوی، تیکت هر دو بازگردانده می‌شود)

`GET /api/wallet/me` موجودی و تاریخچه تراکنش‌های کاربر را برمی‌گرداند.

### محدودیت‌های شناخته‌شده این نسخه

- **ایمیل**: نقاط اطلاع‌رسانی (قفل شدن مسابقه، نیاز به تایید، نتیجه نهایی) همه به یک تابع stub وصل‌اند (`services/notify.js`) که فقط لاگ می‌کند؛ برای ارسال واقعی باید یک سرویس SMTP/ایمیل (مثل nodemailer) وصل شود.
- **آپلود مستندات اختلاف**: فیلد `evidence` فعلاً فقط متن/URL می‌پذیرد، آپلود واقعی فایل/عکس پیاده نشده.
- **کد شارژ / درگاه پرداخت**: خرید تیکت با پول واقعی پیاده نشده؛ فعلاً فقط شارژ دستی توسط ادمین (`POST /api/admin/wallet/:userId/adjust`) وجود دارد.
- **زمان‌بندی فصلی خودکار**: بازنشانی فصلی دستی است (endpoint ادمین)، کرون‌جاب/زمان‌بندی خودکار وجود ندارد.

## نکته درباره مستندات

ریپازیتوری در ابتدا فقط یک قالب خالی Streamlit بود؛ ساخت اولیه فقط بر اساس یک تصویر طراحی صفحه اصلی انجام شد. بعداً دو سند مشخصات کامل (بازی رو-در-رو/کاپ/لیگ/پلی‌آف، اقتصاد تیکت/اکسپرینس، پنل مدیریت چندسطحی و...) ارائه شد که دامنه واقعی پروژه را مشخص کرد. در حال حاضر فقط هسته «رو-در-رو» طبق آن مستندات پیاده‌سازی شده؛ بخش‌های دیگر (کاپ/پلی‌آف/لیگ واقعی، فروشگاه، پنل نمایندگان، فیلدهای پویا و...) هنوز باقی مانده‌اند.
